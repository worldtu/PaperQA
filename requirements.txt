# PaperQA — HF-friendly requirements
# Core
numpy>=1.26.0
scikit-learn>=1.3.0

# Hugging Face stack (embeddings, local models, datasets)
transformers>=4.44.0
datasets>=2.19.0
accelerate>=0.33.0
huggingface-hub>=0.23.0
sentence-transformers>=3.0.1

# Retrieval (vector + keyword)
faiss-cpu>=1.8.0
pyserini==0.40.0    # requires Java 11+ installed

# Parsing / I/O (optional; uncomment as you integrate)
# unstructured>=0.13.0
# grobid-client-python>=0.8.0

# Local LLM serving (optional; choose one)
# ollama>=0.1.4
# vllm>=0.5.0

# Testing
pytest>=8.1.1

# --- IMPORTANT ---
# Install PyTorch separately for your platform BEFORE installing the above:
#   Linux/CPU:    pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cpu
#   macOS (MPS):  pip install torch==2.4.0
#   CUDA 12.1:    pip install torch==2.4.0 torchvision --index-url https://download.pytorch.org/whl/cu121

# --- SearchTool dependencies ---
arxiv
requests
pymupdf
nltk